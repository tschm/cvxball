name: Bump version

on:
  workflow_dispatch

jobs:
  tag:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.tag_version.outputs.tag }}  # This will output the tag for other jobs
    steps:
      - name: Generate Tag
        id: tag_version
        uses: cvxgrp/.github/actions/tag@docker
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Debug Output Tag
        run: |
            echo "Tag: ${{ steps.tag_version.outputs.tag }}"

  docker:
    needs: tag
    permissions:
      contents: read
      packages: write

    runs-on: ubuntu-latest
    steps:
      # -----------------------------------------------------------------------------
      # Step 1: Checkout the repository
      # -----------------------------------------------------------------------------
      - name: Checkout [${{ github.repository }}]
        uses: actions/checkout@v4

      - name: Build and Push Docker Image
        uses: cvxgrp/.github/actions/docker@docker
        with:
          repository: ${{ github.repository }}
          tag: ${{ needs.tag.outputs.tag }}
          dockerfile-path: 'docker/Dockerfile'

      #- name: Output the generated tag
      #  run: |
      #    echo "Github actor: ${{ github.actor }}"
      #    echo "GENERATED TAG: ${{ needs.tag.outputs.tag }}"

      #- name: Install Podman
      #  run: |
      #    sudo apt-get update
      #    sudo apt-get install -y podman

      #- name: Login to GitHub Container Registry
      #  run: |
      #    podman login ghcr.io -u ${{ github.actor }} -p ${{ secrets.GITHUB_TOKEN }}

      #- name: Build container image
      #  run: |
      #    ls -all
      #    pwd
      #    podman build \
      #      --tag ghcr.io/${{ github.repository }}:${{ needs.tag.outputs.tag }} \
      #      -f docker/Dockerfile \
      #      .

      #- name: Push container image
      #  run: |
      #    podman push ghcr.io/${{ github.repository }}:${{ needs.tag.outputs.tag }}
